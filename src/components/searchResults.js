//Verilen sonuclar ile sonuc sayfasi(bolumu) olusturma;
//Gelen array durumuna gore.

export default function createSearchResult(mealList) {
  console.log(mealList);
}

function createmealCard(cardData) {}
